import React from 'react';
import {SafeAreaView, View, Text} from 'react-native';

const Book = (props) => {
  return (
    <SafeAreaView>
      <View>
        <Text>Welcome Book</Text>
      </View>
    </SafeAreaView>
  );
};

export default Book;
